import {FlowViewport} from "../containers/FlowViewport";

export const FlowViewPanel = () => {

    return <div style={{
        height: '100%',
        cursor: 'auto',
    }}>
        <FlowViewport/>
    </div>
}